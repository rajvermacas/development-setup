Dev container setup:
1. Run below command in cmd:
docker run -d ^
--name development-work ^
-p 2222:22 ^
--mount type=volume,source=dev-work,target=/dev-work ^
--mount type=bind,source=C:\Projects,target=/root/projects ^
mrinalrajyoga1/dev-container:v3.0


2. Now exit from the container

3. Run below command in git bash:
ssh-keygen -t rsa -b 4096 -C "rajvermacasual@gmail.com"
ssh-copy-id -i ~/.ssh/id_rsa.pub -p 2222 root@localhost

It's not required to create the ssh key pair again using ssh-keygen if it is already present

4. Vs code
ssh root@localhost:2222

5. Install node using windsurf

6. npm install -g @google/gemini-cli

7. npm install -g @anthropic-ai/claude-code